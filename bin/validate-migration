#!/bin/bash

set -euo pipefail

# Validate migration to ensure migrate -> rollback produces the original schema
# Usage: bin/validate-migration

source bin/lib/loadenv

loadenv-test

bin/lib/clean
bin/lib/build

npx cala-validate-migration
