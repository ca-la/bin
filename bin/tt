#!/bin/bash

set -o errexit
set -o nounset
set -o pipefail
set -o noglob

# Run a single test
# Usage: bin/tt <path-to-spec>

main() {
  source bin/lib/loadenv
  loadenv_test

  npx ts-node $1 | npx tap-spec
}

main "$@"
