#!/bin/bash

set -o errexit
set -o nounset
set -o pipefail
set -o noglob

# Run server with ts-node watching for changes
# Usage: bin/dev

main() {
  source bin/lib/loadenv
  loadenv_local

  npx nodemon -e '[t|j]s'  -w ./src --exec 'ts-node' ./src
}

main "$@"
