#!/bin/bash

set -o errexit
set -o nounset
set -o pipefail
set -o noglob

# Run node REPL with local environment loaded
# Usage: bin/console

run() {
  source bin/lib/loadenv
  loadenv_local

  npx ts-node
}

run "$@"
