#!/bin/bash

set -o errexit
set -o nounset
set -o pipefail
set -o noglob

# Start up node API server
# Usage: bin/start

main() {
  source bin/lib/loadenv
  loadenv_local

  if [ ! -d "dist" ]; then
    echo 'No built application found in dist/ directory'
    exit 1
  fi

  node dist
}

main "$@"
