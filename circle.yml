machine:
  environment:
    DATABASE_URL: postgres://ubuntu:@127.0.0.1:5432/circle_test
  node:
    version: 8.2.1

dependencies:
  pre:
    - npm install -g npm

test:
  pre:
    - bin/migrate-local && bin/rollback-local && bin/migrate-local

deployment:
  staging:
    branch: master
    heroku:
      appname: cala-api-stg
  production:
    branch: production
    heroku:
      appname: cala-api-prod
