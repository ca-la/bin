'use strict';

const createUser = require('../../test-helpers/create-user');

const { get } = require('../../test-helpers/http');
const { test } = require('../../test-helpers/fresh');

test('GET /addresses returns a 403 when called without a user ID', () => {

});

test('GET /addresses returns a 403 when called with someone elses user ID', () => {

});

test('GET /addresses returns a list of addresses', () => {

});
